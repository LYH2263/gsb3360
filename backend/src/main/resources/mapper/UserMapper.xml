<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.campustrade.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.example.campustrade.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="phone" column="phone"/>
        <result property="role" column="role"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="findByUsername" parameterType="string" resultMap="UserResultMap">
        SELECT id, username, password, phone, role, created_at
        FROM users
        WHERE username = #{username}
    </select>

    <select id="findById" parameterType="long" resultMap="UserResultMap">
        SELECT id, username, password, phone, role, created_at
        FROM users
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.example.campustrade.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, password, phone, role, created_at)
        VALUES (#{username}, #{password}, #{phone}, #{role}, NOW())
    </insert>

    <select id="findAll" resultMap="UserResultMap">
        SELECT id, username, password, phone, role, created_at
        FROM users
        ORDER BY created_at DESC
    </select>

    <select id="findPage" resultMap="UserResultMap">
        SELECT id, username, password, phone, role, created_at
        FROM users
        ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM users
    </select>

    <update id="updateRole">
        UPDATE users
        SET role = #{role}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="long">
        DELETE FROM users WHERE id = #{id}
    </delete>

</mapper>
